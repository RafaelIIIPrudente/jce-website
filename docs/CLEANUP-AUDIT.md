# Cleanup audit

Dependency-usage snapshot at cleanup time. Generated by inspecting imports under `app/`, `lib/`, `components/`, and root-level config files (`env.ts`, `instrumentation*.ts`, `*.config.ts`, `.husky/`).

Categories:

- **Used** — at least one direct import in production code (`app/`, `lib/`, `components/`).
- **Wired but inert** — installed and provider-mounted but no consumer code yet. Removing also requires unwiring the mount.
- **Config-only** — only imported by config files, not production code.
- **Unused** — zero imports anywhere. Eligible for removal.

## Dependencies (`package.json` → `dependencies`)

| Package                          | Category          | Evidence                                                                                                     | Recommendation                                                                           |
| -------------------------------- | ----------------- | ------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------- |
| `@ai-sdk/anthropic`              | Unused            | 0 imports                                                                                                    | Remove unless an AI feature is planned soon.                                             |
| `@ai-sdk/openai`                 | Unused            | 0 imports                                                                                                    | Remove unless an AI feature is planned soon.                                             |
| `@hookform/resolvers`            | Unused            | 0 imports (`components/ui/form.tsx` uses `react-hook-form` but not the Zod resolver)                         | Keep — almost certainly needed by `/new-form` and the contact form.                      |
| `@radix-ui/react-label`          | Used              | `components/ui/label.tsx`                                                                                    | Keep.                                                                                    |
| `@radix-ui/react-slot`           | Used              | `components/ui/button.tsx`                                                                                   | Keep.                                                                                    |
| `@react-email/components`        | Unused            | 0 imports                                                                                                    | Remove until the contact form needs HTML email.                                          |
| `@react-email/render`            | Unused            | 0 imports                                                                                                    | Remove until the contact form needs HTML email.                                          |
| `@sentry/nextjs`                 | Used              | `app/error.tsx:5`, `instrumentation.ts:1`, `instrumentation-client.ts:1`, `sentry.{server,edge}.config.ts:1` | Keep — production observability.                                                         |
| `@stripe/stripe-js`              | Unused            | 0 imports                                                                                                    | Remove — no checkout UI planned.                                                         |
| `@supabase/ssr`                  | Used              | `lib/supabase/{client,server,middleware}.ts`                                                                 | Keep.                                                                                    |
| `@supabase/supabase-js`          | Unused            | 0 direct imports (peer dep of `@supabase/ssr`)                                                               | Keep — required as peer by `@supabase/ssr`.                                              |
| `@t3-oss/env-nextjs`             | Config-only       | `env.ts:1`                                                                                                   | Keep — env validation belongs at module root.                                            |
| `@tanstack/react-query`          | Wired but inert   | Mounted in `app/providers.tsx:4` but no `useQuery` calls anywhere                                            | Keep for now; removal also requires unwiring `QueryClientProvider` from `providers.tsx`. |
| `@tanstack/react-query-devtools` | Unused            | 0 imports                                                                                                    | Remove unless devtools are wanted.                                                       |
| `@tanstack/react-table`          | Unused            | 0 imports                                                                                                    | Remove — no data tables on a marketing site.                                             |
| `@upstash/ratelimit`             | Unused            | 0 imports                                                                                                    | Remove unless rate-limiting the contact form.                                            |
| `@upstash/redis`                 | Unused            | 0 imports                                                                                                    | Remove unless rate-limiting the contact form.                                            |
| `@vercel/analytics`              | Unused            | 0 imports                                                                                                    | Keep if you plan to add `<Analytics />` to the root layout; otherwise remove.            |
| `@vercel/flags`                  | Unused            | 0 imports                                                                                                    | Remove — no flag usage planned.                                                          |
| `@vercel/speed-insights`         | Unused            | 0 imports                                                                                                    | Keep if you plan to add `<SpeedInsights />` to the root layout; otherwise remove.        |
| `ai`                             | Unused            | 0 imports                                                                                                    | Remove — see `@ai-sdk/*`.                                                                |
| `class-variance-authority`       | Used              | `components/ui/button.tsx`, other shadcn primitives                                                          | Keep — shadcn dep.                                                                       |
| `clsx`                           | Used              | `lib/utils.ts:1` (via `cn()`)                                                                                | Keep — rule 7.                                                                           |
| `date-fns`                       | Unused            | 0 imports                                                                                                    | Remove until a date is rendered.                                                         |
| `drizzle-orm`                    | Used              | `lib/db/schema.ts`, `lib/db/client.ts`                                                                       | Keep.                                                                                    |
| `gray-matter`                    | Unused            | 0 imports                                                                                                    | Remove — no MDX layer.                                                                   |
| `inngest`                        | Unused            | 0 imports                                                                                                    | Remove — no jobs.                                                                        |
| `lucide-react`                   | Used              | 7 imports across `app/` and `components/`                                                                    | Keep — rule 8.                                                                           |
| `motion`                         | Unused            | 0 imports (no `motion/react` references)                                                                     | Keep if animations are planned for the marketing site; otherwise remove.                 |
| `next`                           | Used              | Framework                                                                                                    | Keep.                                                                                    |
| `next-mdx-remote`                | Unused            | 0 imports                                                                                                    | Remove — no MDX layer.                                                                   |
| `next-themes`                    | Used              | `components/ui/sonner.tsx:3` (Sonner reads the active theme)                                                 | Keep while `sonner.tsx` is present — removal requires un-using `useTheme` there.         |
| `pino`                           | Unused            | 0 imports                                                                                                    | Remove until structured logging is wired.                                                |
| `postgres`                       | Used              | `lib/db/client.ts:4`                                                                                         | Keep.                                                                                    |
| `posthog-js`                     | Used              | `app/providers.tsx:5,7`                                                                                      | Keep.                                                                                    |
| `posthog-node`                   | Unused            | 0 imports                                                                                                    | Remove until server-side analytics events are needed.                                    |
| `radix-ui`                       | Used              | 6 imports across shadcn primitives                                                                           | Keep.                                                                                    |
| `react`                          | Used              | Framework                                                                                                    | Keep.                                                                                    |
| `react-dom`                      | Used              | Framework                                                                                                    | Keep.                                                                                    |
| `react-hook-form`                | Used              | `components/ui/form.tsx:14`                                                                                  | Keep.                                                                                    |
| `rehype-pretty-code`             | Unused            | 0 imports                                                                                                    | Remove — no MDX layer.                                                                   |
| `remark-gfm`                     | Unused            | 0 imports                                                                                                    | Remove — no MDX layer.                                                                   |
| `resend`                         | Unused            | 0 imports                                                                                                    | Keep if the contact form will send email; otherwise remove.                              |
| `sonner`                         | Used              | `components/ui/sonner.tsx`                                                                                   | Keep — wired into the root layout as `<Toaster />`.                                      |
| `stripe`                         | Unused            | 0 imports                                                                                                    | Remove — no payments.                                                                    |
| `tailwind-merge`                 | Used              | `lib/utils.ts:2` (via `cn()`)                                                                                | Keep — rule 7.                                                                           |
| `tw-animate-css`                 | Config-only       | `app/globals.css:2` (`@import`)                                                                              | Keep if shadcn animations are desired; otherwise remove and drop the `@import`.          |
| `zod`                            | Used (transitive) | `env.ts:2`; required at every form/schema boundary                                                           | Keep.                                                                                    |
| `zustand`                        | Unused            | 0 imports                                                                                                    | Remove — no client global state.                                                         |

## DevDependencies (`package.json` → `devDependencies`)

| Package                       | Category    | Evidence                                                | Recommendation                                                                     |
| ----------------------------- | ----------- | ------------------------------------------------------- | ---------------------------------------------------------------------------------- |
| `@playwright/test`            | Unused      | No `playwright.config.ts`, no specs                     | Keep if E2E is planned; otherwise remove with the runner and `playwright install`. |
| `@tailwindcss/postcss`        | Config-only | `postcss.config.mjs`                                    | Keep — Tailwind v4 requires it.                                                    |
| `@testing-library/jest-dom`   | Unused      | No test setup                                           | Remove until Vitest is wired.                                                      |
| `@testing-library/react`      | Unused      | No test setup                                           | Remove until Vitest is wired.                                                      |
| `@types/node`                 | Config-only | TS compiler                                             | Keep.                                                                              |
| `@types/react`                | Config-only | TS compiler                                             | Keep.                                                                              |
| `@types/react-dom`            | Config-only | TS compiler                                             | Keep.                                                                              |
| `@vitejs/plugin-react`        | Unused      | No `vitest.config.ts`                                   | Remove until Vitest is wired.                                                      |
| `dotenv`                      | Config-only | `drizzle.config.ts:1`                                   | Keep — Drizzle Kit needs it.                                                       |
| `drizzle-kit`                 | Config-only | `drizzle.config.ts:2`, `pnpm db:*` scripts              | Keep.                                                                              |
| `eslint`                      | Config-only | `eslint.config.mjs`, `pnpm lint`                        | Keep.                                                                              |
| `eslint-config-next`          | Config-only | `eslint.config.mjs`                                     | Keep.                                                                              |
| `eslint-config-prettier`      | Config-only | Implicit via `prettier` interop                         | Keep.                                                                              |
| `husky`                       | Config-only | `.husky/pre-commit`, `package.json` `prepare` script    | Keep.                                                                              |
| `jsdom`                       | Unused      | No `vitest.config.ts`                                   | Remove until Vitest is wired.                                                      |
| `lint-staged`                 | Config-only | `package.json` `lint-staged` block, `.husky/pre-commit` | Keep.                                                                              |
| `prettier`                    | Config-only | `lint-staged`, `pnpm exec prettier`                     | Keep.                                                                              |
| `prettier-plugin-tailwindcss` | Config-only | Prettier plugin                                         | Keep.                                                                              |
| `react-email`                 | Unused      | CLI for previewing email templates; no templates exist  | Remove with `@react-email/*` unless the contact form will use email templates.     |
| `shadcn`                      | Config-only | CLI used via `pnpm dlx shadcn@latest add ...`           | Optional — `pnpm dlx` works without it being a devDep. Safe to remove.             |
| `tailwindcss`                 | Config-only | `@import "tailwindcss"` in `app/globals.css:1`          | Keep.                                                                              |
| `typescript`                  | Config-only | TS compiler                                             | Keep.                                                                              |
| `vitest`                      | Unused      | No `vitest.config.ts`                                   | Remove until Vitest is wired.                                                      |

## Summary counts

- **Used**: 16 deps + 0 devDeps = **16**
- **Wired but inert**: 1 dep (`@tanstack/react-query`) + 0 devDeps = **1**
- **Config-only**: 4 deps + 14 devDeps = **18**
- **Unused**: 28 deps + 9 devDeps = **37**

## Follow-ups

These are out of scope for this cleanup pass. The user should address each on its own track.

1. **Favicon replacement** — `app/favicon.ico` is the default Next.js icon (25,931 bytes, MS Windows icon resource: 16x16 + 32x32). Replace with a JCE-branded icon. Suggested workflow: design a square mark in `public/icon.png` (512×512) and let Next generate icons from it, or commit a hand-crafted `app/icon.tsx` route.
2. **OG image generation** — `app/og/route.tsx` was intentionally not created. Add when brand design assets exist. Use `next/og`'s `ImageResponse` to render a templated card with the page title and brand mark.
3. **Real page copy** — Six marketing section stubs (`about-us`, `product-services`, `professional-services`, `projects` + 3 sub-pages, `contact-us`) all read "Content for this section is being prepared." Replace with real copy when available.
4. **Confirm `motion` retention** — animations are not yet used. If no motion is planned for the marketing site, drop `motion` and remove the lint rule reference in `.claude/hooks/quality-gate.sh` (rule 9 enforcement is still fine — keeps `framer-motion` from sneaking in).
5. **Confirm Vercel analytics retention** — `@vercel/analytics` and `@vercel/speed-insights` are commonly added to the root layout (`<Analytics />`, `<SpeedInsights />`). If these are wanted, wire them in `app/layout.tsx` and mark Used. If not, remove.
6. **Confirm `@react-email/*` and `resend` retention** — these only become useful when the contact form persists or emails leads. Decide alongside the contact-form implementation.
7. **Test runners** — `vitest`, `@playwright/test`, `@testing-library/*`, `jsdom`, `@vitejs/plugin-react` are all installed but unconfigured. Either wire `vitest.config.ts` + `playwright.config.ts` and commit them, or remove the runners.
8. **Marketing-only deps batch removal** — if you want a one-shot prune, the safe targets are: `@ai-sdk/anthropic`, `@ai-sdk/openai`, `ai`, `@stripe/stripe-js`, `stripe`, `inngest`, `@upstash/ratelimit`, `@upstash/redis`, `@vercel/flags`, `next-mdx-remote`, `gray-matter`, `remark-gfm`, `rehype-pretty-code`, `@tanstack/react-table`, `zustand`, `date-fns`, `pino`, `posthog-node`, `@tanstack/react-query-devtools`. That's 19 deps with zero current usage and no clear near-term need for a corporate marketing site.
